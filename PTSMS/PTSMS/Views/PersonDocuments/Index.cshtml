@model IEnumerable<PTSMSDAL.Models.Enrollment.Operations.PersonDocument>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DocumentType.DocumentTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.CompanyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DocumentURL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DocumentType.DocumentTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.CompanyId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DocumentURL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersonDocumentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersonDocumentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersonDocumentId })
        </td>
    </tr>
}

</table>
