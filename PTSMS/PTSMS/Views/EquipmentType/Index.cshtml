@model IEnumerable<PTSMSDAL.Models.Scheduling.References.EquipmentType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EquipmentTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EquipmentTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EquipmentTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EquipmentTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EquipmentTypeId })
        </td>
    </tr>
}

</table>
