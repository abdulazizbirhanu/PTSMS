@model IEnumerable<PTSMSDAL.Models.Dispatch.ActivityAuthorization>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActivityCheckIn.Comments)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Remark)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityCheckIn.Comments)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remark)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityAuthorizationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityAuthorizationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityAuthorizationId })
        </td>
    </tr>
}

</table>
