@model IEnumerable<PTSMSDAL.Models.Dispatch.Master.DepartureTimeReason>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PreviousDepartureTimeReason.DepartureTimeReasonName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepartureTimeReasonName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EffectiveDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RevisionNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PreviousDepartureTimeReason.DepartureTimeReasonName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DepartureTimeReasonName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EffectiveDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RevisionNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepartureTimeReasonId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepartureTimeReasonId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepartureTimeReasonId })
        </td>
    </tr>
}

</table>
